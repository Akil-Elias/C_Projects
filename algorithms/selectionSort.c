#include <stdio.h>

int ages[] = {3, 67, 9, 1, 28, 14}, min, i, j;
int length = sizeof(ages) / sizeof(ages[0]);

void selectionSort(int arr[]){

    for(i=0; i<length-1; i++){
        min = i;
        for(j=i+1; j<length; j++){
            if()
        }
    }
}


int main(){


    return 0;
}